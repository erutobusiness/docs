# Qiita

Qiitaへの投稿記事、アイデア、下書きを管理するディレクトリ。

## 運用ルール

- 技術的な知見（Tech）の中から、外部発信（Output）するものをここで扱う
- `docs/tech` 以下のメモを元に記事化する場合も、執筆作業はここで行う
- 文体は「ですます調」を基本とする（`.textlintrc.js` で設定済み）

## ディレクトリ構成

- 記事ごとにフォルダを作成することを推奨する（画像リソースなどをまとめるため）
- 投稿済み記事は `published` などのフォルダで整理するか、タグ付けで管理する
